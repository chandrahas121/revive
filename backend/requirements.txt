Django==4.2.13
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
python-dotenv==1.0.1
Pillow>=11.0.0
dj-database-url==2.1.0
psycopg2-binary==2.9.9
# Production web server (replaces dev runserver)
gunicorn==21.2.0
# Serves CSS/JS files efficiently without a separate nginx for static files
whitenoise==6.6.0
# Redis cache backend for Django
django-redis==5.4.0
# Celery — production async task queue for ML grading pipeline
celery[redis]==5.3.6
# AWS S3 image storage (boto3 is pulled in as a dependency)
django-storages[s3]==1.14.4

# Lightweight ML dependencies for OpenRouter API and recommendation logic
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.1
scikit-learn>=1.3.0
lightgbm>=4.0.0
implicit>=0.7.0
openai>=1.10.0
requests>=2.31.0
qrcode[pil]>=7.4.0

# Dependencies for Virtual Try-On via HuggingFace
gradio_client>=0.16.0
