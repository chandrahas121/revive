"""
Fit-Twin — return prevention by matching a shopper to real people built like them.

Public surface:
    build_index.build(...)          # offline: dataset -> artifact
    match.find_fit_twins(...)       # online: body + item/category -> twins + size guidance
"""
