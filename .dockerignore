# Python
**/__pycache__/
**/*.pyc
**/*.pyo
**/.pytest_cache/
**/*.egg-info/

# Virtual environments
.venv/
venv/
env/

# Secrets — never bake .env (prod keys) into the image. The container gets its
# config from docker-compose `environment:` instead.
.env
.env.*
backend/.env
backend/.env.*

# Django
backend/db.sqlite3
backend/staticfiles/
backend/media/

# ML artifacts that are large (the grade cache and FAISS index can be large)
ml/artifacts/grade_cache.json

# Frontend (built separately)
frontend/node_modules/
frontend/dist/

# Git
.git/
temp_clone/

# Docs / misc
amazon_submisstion/
*.md
images/
